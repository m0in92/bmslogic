Tests pertaining to functionality based purely in Python or those that uses Python as the interface are stored in the tests folder. They can be run using Pytest using the following command on the terminal at the project root directory.
```sh
pytest tests
```